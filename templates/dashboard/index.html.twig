{% extends 'base.html.twig' %}

{% block title %}{% endblock %}

{% block body %}
<div class="d-flex flex-column article-list">

    {% for article in articles %}
        <div class="col-6">
            <h2>{{article.titre}}</h2>
            <a href='blog/{{article.idArticle}}+{{article.slug}}'>Voir l'article</button>
            </a>
        </div>
    {% endfor %}

</div>

{% endblock %}
