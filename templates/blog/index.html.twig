{% extends 'base.html.twig' %}

{% block title %}{{ article.titre }}{% endblock %}

{% block body %}
    <h1>{{ article.titre }}</h1>
    <img src="{{article.img}}" class="img-fluid">
    <p>{{article.contenu}}</p>

{% endblock %}
